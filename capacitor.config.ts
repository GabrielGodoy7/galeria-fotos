import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'galeria-fotos',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
